Check for browser updates, network issues, and correct URLs before browser automation; ensure a stable internet connection.  
Verify clickable elements are visible, enabled, and interactive before clicking; use appropriate wait functions.  
Confirm successful task completion (e.g., file downloads) before proceeding to subsequent steps.  
If a tool or action fails, review error messages, ensure required elements are present and interactive, and retry navigation or actions when appropriate.  
Set the browser to the correct language before downloading or filling language-dependent forms.  
Ensure tools are properly installed, registered, and available before executing commands; if execution fails, check the list of available tools.  
Confirm the Python `cv2` (OpenCV) module is installed; use `pip install opencv-python` if needed.  
Grant necessary permissions (e.g., camera access) when requested to avoid access denial.  
When checking for unread emails, avoid sending, deleting, or modifying emails unless explicitly requested.  
Verify that folder paths for Downloads and Pictures exist and are correct before opening them or searching for files.  
Ensure a compatible media player is installed before opening .mp4 files with the open_item tool.  
When using the open_item tool, ensure the target browser is correctly specified and installed.  
Ensure AVA navigates to the intended website and verify the correct page has loaded; if navigation fails, retry.  
When searching for files in Gmail, look for specific file names or attachments and, if not found, confirm details (name, location) with the user.  
Recognize that the error message “this browser or app may not be secure” often indicates an outdated browser or restrictive security settings.
