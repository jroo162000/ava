Verify the status and configuration of the browser automation tool if it behaves unexpectedly.  
Ensure the action is valid and provide a valid selector for the element when using browser automation.  
Clarify user preferences regarding naming and pronunciation to avoid confusion.  
Directly address the user's question to avoid confusion when responding.  
Break down complex tasks into clear steps to facilitate user understanding.  
Provide summaries or documents as requested by the user.  
Ensure created documents or lists are easily accessible in the Downloads folder.  
Confirm the creation of a file when requested and assist in locating it if necessary.  
Ensure the browser is updated to the latest version and check for network issues before running automation tasks.  
Ensure the element you are trying to click is visible and enabled before attempting the action; use a wait function if necessary.  
Confirm the successful completion of tasks, such as downloading files, before proceeding.  
Check for errors and ensure the element is interactable when a tool fails to perform an action.  
If the automation tool cannot interact with an element, verify its visibility and interactivity.  
Ensure the browser is set to the correct language before attempting to download forms.
Lesson: When using window_ops, ensure the window title matches an open window. Check for correct titles before attempting to close a window.
Lesson: When closing a browser window, ensure the title matches exactly, including special characters and spaces, to avoid "No window found" errors.
Lesson: When using window_ops, ensure the window title is correct and matches the exact name of the open window to avoid "No window found" errors.
