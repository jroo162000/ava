# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
PIPFILE.lock

# Virtual Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Testing
coverage/
.nyc_output/
*.lcov

# Production builds
/dist
/build

# Misc
.cache/
.parcel-cache/
.next/
.nuxt/
.vuepress/dist
.serverless/
.fusebox/
.dynamodb/
.tern-port

# AVA Specific
memory/*.json
!memory/heartbeat-state.json
vendor/piper/*.zip
data/
archive/

# Model files (large binaries)
*.onnx
*.ort
*.pt
*.pth
*.h5
*.pb
*.tflite
*.mlmodel
vosk-models/
voices/
# NOTE: do NOT ignore "voice/" — ava-integration/voice/ is the voice-runner SOURCE
# scaffold (piper_bin, edge_stream, elevenlabs_tts, bus, session, providers) that
# ava_local_voice.py imports. Models stay ignored via *.onnx / voices/ / vendor/piper/models/.

# Temporary files
*.tmp
*.temp
*.pid
*.seed
*.pid.lock
tmp/
temp/

# === Session cleanup (added 2026-06-26) ===
# Archives & backups
*.zip
*.bak
*.bak_*
*.pass1bak
*.pass2bak
*.manual_recovery_backup_*

# Audio & training datasets (regenerable / large binaries)
*.wav
vella_dataset/
vendor/piper/models/

# Latency / trace / run logs
*.jsonl
latency.jsonl
run_out.txt
gen_log.txt
gen_err.txt

# Throwaway trace & scratch dirs
tmp_*/
tmp_*
ava_test_tmp/
ava_session_helpers/
sandbox/

# Test output
jest-results.json

# Stray artifacts
nul
.write_test_tmp

# Auto-agent runtime state (regenerated each run)
ava-integration/training/

# Separate sub-app (its own project; contains its own .env)
AVa's reasoning/

# === Secrets — NEVER commit (API keys, credentials, certs) ===
# (Belt-and-suspenders on top of the .env rule above.)
.env
.env.*
!.env.template
!.env.example
**/secrets.json
**/secret.json
*secrets*.json
*.secret
**/credentials.json
**/credential.json
*.pem
*.key
*.p12
*.pfx
*.pkcs12
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.keystore
*.jks
service-account*.json
gcp-*.json
*apikey*
*api_key*

# === Self-snapshot backups (regenerable; duplicate code) ===
ava-integration/backup/
backup_*/
**/snapshots/
